package com.rhmn.learneng.data.model

enum class DayResult {
    FAIL,
    SUCCESS,
    OPEN,
    LOCK
}

enum class DayStep{
    VOCAL, DICT, PRO, GR_QUIZ, READ, LISTEN, FNL_QUIZ , FINISH
}

enum class DayType {
    DAY_1, DAY_2, DAY_3
}